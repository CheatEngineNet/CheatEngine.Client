<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>14.0</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <PlatformTarget>x64</PlatformTarget>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <!-- Enables Hosting's complete direct-reference, plugin-shape, language, and architecture profile. -->
    <CheatEngineClientPluginProject>true</CheatEngineClientPluginProject>
    <IsAotCompatible>true</IsAotCompatible>
    <VerifyReferenceAotCompatibility>true</VerifyReferenceAotCompatibility>
    <!-- A generated template is standalone even when it is created under this repository. -->
    <ManagePackageVersionsCentrally>false</ManagePackageVersionsCentrally>
  </PropertyGroup>

  <!--
    Keep CheatEngine.SDK on 1.x: this template is built and tested against CheatEngine.SDK 1.0.0 and CheatEngine.Client
    declares [1.0.0, 2.0.0). The packed template references the exact CheatEngine.Client version it was released with.
  -->
  <ItemGroup>
    <PackageReference Include="CheatEngine.Client" Version="0.1.0"/>
    <PackageReference Include="CheatEngine.SDK" Version="1.0.0"/>
    <PackageReference Include="Microsoft.Extensions.Configuration.Json" Version="10.0.12"/>
  </ItemGroup>

  <ItemGroup>
    <None Update="appsettings.json" CopyToOutputDirectory="PreserveNewest"/>
  </ItemGroup>

</Project>
