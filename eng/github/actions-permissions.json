{
  "_comment": "GitHub Actions: every action must be pinned to a full commit SHA (the workflows already are; the contract tests enforce it), the default GITHUB_TOKEN is read-only and workflows cannot approve pull requests.",
  "permissions": {
    "enabled": true,
    "allowed_actions": "all",
    "sha_pinning_required": true
  },
  "workflow": {
    "default_workflow_permissions": "read",
    "can_approve_pull_request_reviews": false
  }
}
